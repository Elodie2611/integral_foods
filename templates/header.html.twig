
<head>
<link rel="stylesheet" href="{{ asset('build/app.css') }}"
</head>
    <header>

    <div class="container-fluid " style="background-image: url({{ asset('build/images/ban_5.jpg') }})" id="banniere_nav">
                      
        <nav class="main_nav row">

            <div class="row col-2 mt-2"> <a href="/"> <img src="{{ asset('build/images/brigadesmall.png') }}" alt="logo_brigade" id="logo"></a></div>
      
        {% if is_granted('ROLE_troll') %}
                    <div class="row text-center mt-4">
                   <ul class="row" id="liens_nav">
                            <li ><a href="/logout"><button class="logout">Déconnexion</button></a></li>
                      </ul>
                  </div>
                {% else %}
                   {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <div class="row text-center mt-4">
                   <ul class="row" id="liens_nav">
                            <li><a href="/logout"><button class="logout">Déconnexion</button></a></li>
                      </ul>
                  </div>
                {% endif %}
                   {% if email is defined %}
                        <div class="row buttons col-8 justify-content-end mt-4">

                            <ul class="row">
                                <li><a href="/panier" ><i class="fas fa-shopping-basket fa-lg text-light" ></i></a></li>
                           
                                <li><a href="utilisateur/profil/{{ email }}" ><i class="far fa-user fa-lg text-light" ></i></a></li>
                                     
                                <li><a href="mailto:labrigade@integralfoods.fr" ><i class="fas fa-envelope fa-lg text-light" ></i></a></li>
                            </ul>   

                        </div>                  

                    </nav>
                  {% endif %}
                {% endif %}
            </div>

    </header>